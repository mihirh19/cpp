#include <iostream>
#include <vector>
using namespace std;

int main()
{
    long long t;
    cin>>t;
    while (t--)
    {
        long long n,  s = 0;
        cin>>n;
        vector<long long>v(n);

        for (int i = 0; i < 7n; i++)
        {
            long long x;
            cin>>x;
            v.push_back(x);
            s = s +x;
        }
        if ( find(v.begin(), v.end().)  )
        {
            /* code */
        }
        
        
    }
    
    return 0;
}
